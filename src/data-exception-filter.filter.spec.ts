import { DataExceptionFilterFilter } from './data-exception-filter.filter';

describe('DataExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new DataExceptionFilterFilter()).toBeDefined();
  });
});
