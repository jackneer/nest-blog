import { ExecutionInterceptor } from './execution.interceptor';

describe('ExecutionInterceptor', () => {
  it('should be defined', () => {
    expect(new ExecutionInterceptor()).toBeDefined();
  });
});
